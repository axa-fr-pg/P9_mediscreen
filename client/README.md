# Client
The client module contains the required front end for mediscreen.

# Build
To install the module, run the maven goal "install".
This module can be installed directly from the parent project.

# Run
To start the client, open page http://localhost:5000.
